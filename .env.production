# Production Environment Variables
# Used when running services in Docker containers

# ================================
# Backend Configuration (FastAPI)
# ================================
APP_NAME=Awesome API
ADMIN_EMAIL=admin@example.com
ITEMS_PER_USER=50
ENVIRONMENT=production

# Database Configuration (Docker PostgreSQL)
DB_HOST=db
DB_PORT=5432
DB_NAME=web_site
DB_USER=postgres
DB_PASSWORD=88888888

# Database URL for SQLAlchemy (Docker service names)
DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}

# FastAPI Security
SECRET_KEY=your_super_secret_key
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# FastAPI Server Configuration
API_HOST=0.0.0.0
API_PORT=8000

# ================================
# Frontend Configuration (Next.js)
# ================================
COMPANY_NAME="Your Company"
SITE_NAME="Your Site"

# Shopify Configuration (if used)
SHOPIFY_REVALIDATION_SECRET=""
SHOPIFY_STOREFRONT_ACCESS_TOKEN=""
SHOPIFY_STORE_DOMAIN=""

# Next.js Configuration for Docker
NEXT_PUBLIC_BASE_PATH=
NEXT_PUBLIC_ASSET_PREFIX=

# API Configuration for Frontend (Docker)
# настройки для браузера
# здесь браузер делает запросы к api к контейнеру
# NEXT_PUBLIC_API_URL=  # Commented out to use relative URLs via Traefik

# настройки для серверной части next js
API_BASE_URL=http://app:8000
API_PROTOCOL=http
API_HOSTNAME=app

# CORS Configuration (Docker)
# For production: requests come from same domain via Traefik (no CORS needed for same-origin)
# For development: allow localhost origins
# Add your production domain here if API and frontend are on different domains
CORS_ORIGINS=http://localhost:3000,http://localhost:8000

# Environment
NODE_ENV=production

# ================================
# Docker Service Names (for reference)
# ================================
# Backend service: app
# Frontend service: web
# Database service: db
# Reverse proxy: traefik
